import { checks } from "./editorAi";

const descriptions: Record<keyof typeof checks, string> = {
  unused: "Find declarations that no longer serve a purpose.",
  loops: "Spot avoidable iterations and costly lookups.",
  repeated: "Find calculations that could run just once.",
  correctness: "Look for bugs, boundary cases and unsafe assumptions.",
  errors: "Catch swallowed exceptions and missing recovery.",
  secrets: "Flag credential-like values embedded in code.",
  readability: "Simplify duplication and hard-to-follow logic.",
  tests: "Suggest useful coverage for changed behavior.",
};

export function checklistHtml(nonce: string): string {
  return `<!DOCTYPE html><html lang="en"><head><meta charset="UTF-8">
  <meta http-equiv="Content-Security-Policy" content="default-src 'none'; style-src 'nonce-${nonce}'; script-src 'nonce-${nonce}';">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <style nonce="${nonce}">
  *{box-sizing:border-box}body{margin:0;font:13px/1.5 var(--vscode-font-family);background:var(--vscode-editor-background);color:var(--vscode-foreground)}main{max-width:760px;margin:auto;padding:26px 22px 12px}.brand{display:flex;align-items:center;gap:9px;font-size:11px;font-weight:700;letter-spacing:1.7px}.mark{display:grid;place-items:center;width:27px;height:29px;border-radius:9px;background:var(--vscode-button-background);color:var(--vscode-button-foreground);font-size:16px}.eyebrow{color:var(--vscode-descriptionForeground);font-size:10px;text-transform:uppercase;letter-spacing:1.4px;font-weight:600}h1{font-size:26px;line-height:1.2;letter-spacing:-.8px;margin:22px 0 8px}h2{font-size:14px;margin:0;font-weight:600}p{margin:6px 0}.muted,small{color:var(--vscode-descriptionForeground)}.card{border:1px solid var(--vscode-panel-border,var(--vscode-widget-border));border-radius:12px;padding:16px;margin:22px 0;background:var(--vscode-sideBar-background)}.heading{display:flex;align-items:center;justify-content:space-between;gap:10px;margin-bottom:12px}.badge{border:1px solid var(--vscode-panel-border);border-radius:20px;padding:3px 8px;font-size:10px;white-space:nowrap;color:var(--vscode-descriptionForeground)}.badge.connected{color:var(--vscode-testing-iconPassed)}label.key-label{display:block;font-size:12px;font-weight:600;margin-bottom:7px}.key-row{display:flex;gap:8px;flex-wrap:wrap}input[type=password]{min-width:100px;flex:1;padding:10px 11px;border-radius:7px;border:1px solid var(--vscode-input-border,var(--vscode-panel-border));background:var(--vscode-input-background);color:var(--vscode-input-foreground);font:inherit}button{font:inherit;cursor:pointer;border-radius:7px;border:1px solid transparent;padding:9px 13px;background:var(--vscode-button-background);color:var(--vscode-button-foreground);font-weight:600}button:hover{background:var(--vscode-button-hoverBackground)}button:disabled{opacity:.45;cursor:default}.quiet{padding:4px 0;background:transparent;color:var(--vscode-textLink-foreground);font-size:11px}.quiet:hover{background:transparent;text-decoration:underline}button:focus-visible,input:focus-visible{outline:2px solid var(--vscode-focusBorder);outline-offset:3px}.help{font-size:11px;margin-top:9px}.connection-info{overflow-wrap:anywhere;font-size:12px;margin-top:10px}.check-heading{margin-top:26px}.checklist{border:1px solid var(--vscode-panel-border,var(--vscode-widget-border));border-radius:12px;overflow:hidden;margin-top:12px}.check{display:flex;align-items:center;gap:14px;padding:13px 15px;border-bottom:1px solid var(--vscode-panel-border);cursor:pointer}.check:last-child{border-bottom:0}.check:hover{background:var(--vscode-list-hoverBackground)}.copy{flex:1;min-width:0}.title{display:block;font-size:12px;font-weight:600}.description{display:block;font-size:11px;margin-top:3px;color:var(--vscode-descriptionForeground)}.switch{position:relative;flex:none;width:34px;height:20px}.switch input{position:absolute;opacity:0;width:100%;height:100%;margin:0;cursor:pointer}.track{display:block;width:34px;height:20px;border-radius:20px;background:var(--vscode-input-background);border:1px solid var(--vscode-descriptionForeground);pointer-events:none;transition:background .15s}.track:after{content:'';display:block;width:12px;height:12px;border-radius:50%;background:var(--vscode-descriptionForeground);margin:3px;transition:transform .15s}input:checked+.track{background:var(--vscode-button-background);border-color:var(--vscode-button-background)}input:checked+.track:after{background:var(--vscode-button-foreground);transform:translateX(14px)}input:focus-visible+.track{outline:2px solid var(--vscode-focusBorder);outline-offset:3px}.footer{position:sticky;bottom:0;background:var(--vscode-editor-background);border-top:1px solid var(--vscode-panel-border);padding:14px 22px;margin-top:24px}.footer-inner{max-width:716px;margin:auto}.file{font-size:11px;margin-bottom:10px;overflow-wrap:anywhere}.file strong{font-weight:500}.actions{display:flex;gap:8px}#review{flex:1}.secondary{background:var(--vscode-button-secondaryBackground);color:var(--vscode-button-secondaryForeground)}#status{font-size:11px;color:var(--vscode-descriptionForeground);margin:10px 0 0;overflow-wrap:anywhere}.privacy{font-size:10px;line-height:1.6;margin-top:14px;color:var(--vscode-descriptionForeground)}article{padding:14px;border:1px solid var(--vscode-panel-border);border-radius:10px;margin-top:12px}article h3{font-size:13px;margin:0 0 8px}article p{font-size:12px;white-space:pre-wrap;overflow-wrap:anywhere}#results:empty{display:none}@media(max-width:360px){main{padding:18px 14px 8px}.footer{padding:12px 14px}.check{padding:12px}.description{font-size:10px}h1{font-size:23px}}@media(prefers-reduced-motion:reduce){*{transition:none!important}}
  input[type=text]{width:100%;padding:10px 11px;border-radius:7px;border:1px solid var(--vscode-input-border,var(--vscode-panel-border));background:var(--vscode-input-background);color:var(--vscode-input-foreground);font:inherit}details{margin-top:12px}summary{cursor:pointer;color:var(--vscode-textLink-foreground);font-size:12px}details .key-label{margin-top:10px}
  .scope-section{margin-top:24px}.scope-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:8px;margin:12px 0}.scope-option{position:relative;cursor:pointer;min-width:0}.scope-option input{position:absolute;opacity:0;width:1px;height:1px}.scope-face{display:block;height:100%;padding:12px;border:1px solid var(--vscode-widget-border,var(--vscode-panel-border));border-radius:9px;background:var(--vscode-sideBar-background);transition:border-color .15s,background .15s}.scope-face strong{display:flex;align-items:center;justify-content:space-between;font-size:12px;font-weight:600}.scope-face small{display:block;font-size:10px;line-height:1.5;margin-top:4px}.scope-face strong:after{content:'';width:10px;height:10px;border:1px solid var(--vscode-descriptionForeground);border-radius:50%;flex-shrink:0;margin-left:6px}.scope-option input:checked+.scope-face{border-color:var(--vscode-focusBorder);background:var(--vscode-list-inactiveSelectionBackground)}.scope-option input:checked+.scope-face strong:after{background:var(--vscode-focusBorder);border-color:var(--vscode-focusBorder);box-shadow:inset 0 0 0 2px var(--vscode-editor-background)}.scope-option input:focus-visible+.scope-face{outline:2px solid var(--vscode-focusBorder);outline-offset:2px}.scope-option:hover .scope-face{border-color:var(--vscode-focusBorder)}.scope-option:last-child{grid-column:1/-1}.scope-note{font-size:11px;color:var(--vscode-descriptionForeground);line-height:1.6}.footer{box-shadow:0 -8px 24px #00000012}.file{padding:9px 11px;border:1px solid var(--vscode-panel-border);border-radius:8px;margin-bottom:12px;background:var(--vscode-sideBar-background)}#review{min-height:40px;border-radius:8px;font-size:12px;letter-spacing:.15px}#status{font-size:11px;line-height:1.5}.privacy-details{margin:18px 0 0;padding-top:12px;border-top:1px solid var(--vscode-panel-border)}.privacy-details summary{color:var(--vscode-descriptionForeground);font-size:11px}.card{margin:18px 0}.check{gap:10px}.check .copy{overflow-wrap:anywhere}.scope-grid input:disabled+.scope-face{opacity:.55;cursor:default}@media(max-width:280px){.scope-grid{grid-template-columns:1fr}.brand{letter-spacing:.6px}.brand .badge{display:none}}
  </style></head><body><main>
  <div class="brand"><span class="mark" aria-hidden="true">✓</span> VERIREVIEW <span class="badge">AI REVIEW</span></div>
  <h1>A second look.<br>Before you ship.</h1><p class="muted">Your code. Your AI. Your checklist.</p>
  <section class="card" aria-labelledby="connect-title"><div class="heading"><h2 id="connect-title">Connect your AI</h2><span id="badge" class="badge">NOT CONNECTED</span></div>
  <label class="key-label" for="key">API key</label><div class="key-row"><input id="key" type="password" placeholder="Paste your API key" autocomplete="off" spellcheck="false"><button id="save">Connect</button></div>
  <p class="help muted">OpenAI · Claude · Grok · Groq. We detect the provider and choose a compatible model for you.</p>
  <details id="custom"><summary>Custom connection</summary>
  <p class="help muted">For services that support OpenAI Chat Completions with Bearer API keys. Your key and reviewed code will be sent to the URL you enter. Other API formats are not supported.</p>
  <label class="key-label" for="baseUrl">API base URL (include /v1 if required)</label><div class="key-row"><input id="baseUrl" type="text" placeholder="https://your-provider.example/v1" autocomplete="off"></div>
  <label class="key-label" for="customModel">Model ID</label><div class="key-row"><input id="customModel" type="text" placeholder="Model from your provider" autocomplete="off"></div>
  <p class="help muted">Leave this section open to save a custom connection. Access is checked on your first review. Close it to use automatic key detection.</p></details>
  <p id="connection" class="connection-info" role="status">Your key is stored securely in VS Code.</p><button id="delete" class="quiet" hidden>Disconnect and remove key</button></section>
  <div class="heading check-heading"><div><span class="eyebrow">Make it your review</span><h2>What should we check?</h2></div><span id="count" class="badge">8 selected</span></div>
  <div class="checklist" role="group" aria-label="Review checklist">${Object.entries(
    checks,
  )
    .map(
      ([id, title]) =>
        `<label class="check"><span class="copy"><span class="title">${title}</span><span class="description">${descriptions[id as keyof typeof checks]}</span></span><span class="switch"><input type="checkbox" role="switch" value="${id}" aria-label="${title}" checked><span class="track" aria-hidden="true"></span></span></label>`,
    )
    .join("")}</div>
  <section class="card" aria-labelledby="custom-check-title"><div class="heading"><h2 id="custom-check-title">Your custom checks</h2><span class="badge">PERSONAL</span></div>
  <p class="help muted">Add a rule for your code, such as “Every database query must use parameters.” Up to 20 checks, 500 characters each.</p>
  <label class="key-label" for="newCheck">What should the AI look for?</label><input id="newCheck" type="text" maxlength="500" placeholder="Describe your review rule" autocomplete="off">
  <button id="addCheck" class="quiet">+ Add check</button><p id="customCheckStatus" role="status" class="help"></p>
  <div id="customChecks" role="group" aria-label="Custom review checks"></div></section>
  <section class="scope-section" aria-labelledby="scope-title"><span class="eyebrow">Choose your focus</span><h2 id="scope-title">Review scope</h2>
  <div class="scope-grid" role="radiogroup" aria-labelledby="scope-title">${[
    ["editor", "Current editor", "Full file, including unsaved edits"],
    ["working", "Git changes", "All saved changes since your last commit"],
    ["staged", "Staged changes", "Only what you have staged to commit"],
    ["branch", "Current branch", "Committed changes against your base branch"],
    ["file", "Current file diff", "Saved Git changes in the active file"],
  ].map(([value, title, description]) => `<label class="scope-option"><input type="radio" name="scope" value="${value}" ${value === "editor" ? "checked" : ""}><span class="scope-face"><strong>${title}</strong><small>${description}</small></span></label>`).join("")}</div>
  <p id="scopeNote" class="scope-note">Reviews the full editor contents. Findings appear here and in Problems.</p></section>
  <details class="privacy-details"><summary>What is sent to your AI?</summary><p class="privacy">Editor review sends the current file, including unsaved edits. Git review sends saved diffs. Both include your selected checklist instructions. Provider usage charges apply. AI suggestions are not independently verified.</p></details>
  <section id="results" aria-label="Review findings"></section></main>
  <footer class="footer"><div class="footer-inner"><div class="file"><span class="eyebrow">CURRENT FILE</span><br><strong id="target">Open a source file to review</strong></div><div class="actions"><button id="review" disabled>Review code →</button><button id="cancel" class="secondary" hidden>Cancel</button></div><p id="status" role="status" aria-live="polite">Connect your API key to get started.</p></div></footer>
  <script nonce="${nonce}">
  const vscode=acquireVsCodeApi(),el=id=>document.getElementById(id);let connected=false,busy=false,connecting=false;
  let customChecks=[];
  const preferences=()=>({checks:[...document.querySelectorAll('input[role=switch]:checked')].map(e=>e.value),customChecks});
  function persist(){update();vscode.postMessage({type:'preferences',settings:preferences()});}
  function renderCustom(selected){el('customChecks').replaceChildren();for(const item of customChecks){const row=document.createElement('div');row.className='check';const label=document.createElement('label');label.className='copy';label.textContent=item.description;label.htmlFor=item.id;const toggle=document.createElement('span');toggle.className='switch';const input=document.createElement('input');input.type='checkbox';input.setAttribute('role','switch');input.setAttribute('aria-label',item.description);input.id=item.id;input.value=item.id;input.checked=selected.includes(item.id);input.onchange=persist;const track=document.createElement('span');track.className='track';track.setAttribute('aria-hidden','true');toggle.append(input,track);const remove=document.createElement('button');remove.className='quiet';remove.textContent='Remove';remove.setAttribute('aria-label','Remove '+item.description);remove.onclick=()=>{const selected=preferences().checks;customChecks=customChecks.filter(c=>c.id!==item.id);renderCustom(selected);persist();};row.append(label,toggle,remove);el('customChecks').append(row);}}
  el('addCheck').onclick=()=>{const description=el('newCheck').value.trim();if(!description){el('customCheckStatus').textContent='Describe the check before adding it.';return;}if(customChecks.length>=20){el('customCheckStatus').textContent='You can save up to 20 custom checks.';return;}if(customChecks.some(c=>c.description.toLowerCase()===description.toLowerCase())){el('customCheckStatus').textContent='That check already exists.';return;}const selected=preferences().checks;const id='custom_'+crypto.randomUUID();customChecks.push({id,description});renderCustom([...selected,id]);el('newCheck').value='';el('customCheckStatus').textContent='Check added and enabled.';persist();};
  el('newCheck').onkeydown=e=>{if(e.key==='Enter')el('addCheck').click();};
  function update(){const count=preferences().checks.length;el('count').textContent=count+' selected';el('review').disabled=!connected||busy||connecting||count===0;el('review').textContent=busy?'Reviewing…':'Review code →';el('save').disabled=busy||connecting;el('save').textContent=connecting?'Connecting…':'Connect';el('delete').disabled=busy||connecting;el('cancel').hidden=!busy;}
  document.querySelectorAll('input[role=switch]').forEach(e=>e.onchange=()=>{update();vscode.postMessage({type:'preferences',settings:preferences()});});
  el('save').onclick=()=>{vscode.postMessage({type:'saveKey',key:el('key').value,custom:el('custom').open,baseUrl:el('baseUrl').value,model:el('customModel').value});el('key').value='';};el('key').onkeydown=e=>{if(e.key==='Enter'&&!el('save').disabled)el('save').click();};
  el('delete').onclick=()=>vscode.postMessage({type:'deleteKey'});
  const selectedScope=()=>document.querySelector('input[name=scope]:checked').value;
  document.querySelectorAll('input[name=scope]').forEach(input=>input.onchange=()=>{el('scopeNote').textContent=selectedScope()==='editor'?'Reviews the full editor contents. Findings appear here and in Problems.':'Save your files first. Reviews diffs using your selected checks. Findings appear in Problems and VeriReview AI Git Output.';});
  el('review').onclick=()=>vscode.postMessage({type:'review',settings:preferences(),scope:selectedScope()});
  el('cancel').onclick=()=>vscode.postMessage({type:'cancel'});
  window.addEventListener('message',({data:m})=>{
    if(m.type==='settings'&&m.settings){customChecks=m.settings.customChecks||[];renderCustom(m.settings.checks);document.querySelectorAll('input[role=switch]').forEach(e=>e.checked=m.settings.checks.includes(e.value));update();}
    if(m.type==='target')el('target').textContent=m.name;
    if(m.type==='connection'){connected=!!m.connection;const custom=connected&&m.connection.provider==='custom';el('badge').textContent=connected?(custom?'SAVED':'CONNECTED'):'NOT CONNECTED';el('badge').className='badge'+(connected?' connected':'');el('delete').hidden=!connected;el('connection').textContent=connected?({openai:'OpenAI',claude:'Claude',grok:'Grok',groq:'Groq',custom:'Custom'}[m.connection.provider]+' · '+m.connection.model+(custom?' · '+m.connection.baseUrl+' · Access not yet verified':' · Auto-selected')):'Your key is stored securely in VS Code.';if(custom){el('custom').open=true;el('baseUrl').value=m.connection.baseUrl;el('customModel').value=m.connection.model;}el('status').textContent=connected?'Ready when you are. Select your checks below.':'Connect your API key to get started.';update();}
    if(m.type==='connectionError'){el('connection').textContent=m.text;el('badge').textContent=connected?'PREVIOUS KEY SAVED':'CONNECTION FAILED';}
    if(m.type==='status')el('status').textContent=m.text;
    if(m.type==='busy'){busy=m.value;update();}if(m.type==='connecting'){connecting=m.value;update();}
    if(m.type==='results'){el('results').replaceChildren();m.findings.forEach((f,index)=>{const article=document.createElement('article'),title=document.createElement('h3');title.textContent=f.severity+' · '+f.title;article.append(title);for(const text of ['Line '+f.line,f.explanation,'Suggestion: '+f.suggestion]){const p=document.createElement('p');p.textContent=text;article.append(p);}const button=document.createElement('button');button.className='quiet';button.textContent='Go to line '+f.line+' →';button.onclick=()=>vscode.postMessage({type:'open',index});article.append(button);el('results').append(article);});}
  });vscode.postMessage({type:'ready'});
  </script></body></html>`;
}
